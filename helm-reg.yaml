
projectedSecret:
  - secret:
      name: "properties-2020-11-30"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "lms-lti-crosslist-reg.apps-test.iu.edu"
      paths: []
