
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-snd

env:
  APP_ENV: snd

ingress:
  enabled: true
  hosts:
    - host: "lms-lti-crosslist-snd.apps-snd.iu.edu"
      paths: []

activeProfiles: "redis-session,snd,redis-cache"

serviceAccountName: lms-lti-crosslist-snd

cluster: es-test
